import React, { FC } from 'react'
import { Button } from '../basics'

export const CustomComponent: FC = () =>
  <span>My custom component text</span>

export const ButtonWrapper = () =>
  <>
    <Button key={1} />

  </>

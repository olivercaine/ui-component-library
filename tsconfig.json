{
  "extends": "@olliecaine/tsconfig",
  "compilerOptions": { // https://www.typescriptlang.org/tsconfig
    "allowJs": true,
    "declaration": true, // Generate .d.ts files for every TypeScript or JavaScript file inside your project. These .d.ts files are type definition files which describe the external API of your module.
    "declarationDir": "dist", // Define where declaration files are emitted. - https://www.typescriptlang.org/tsconfig#skipLibCheck
    "esModuleInterop": true, // Import non-ES modules as default imports.
    "jsx": "react", // Controls how JSX constructs are emitted in JavaScript files - https://www.typescriptlang.org/tsconfig#jsx
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ],
    "module": "esnext", // Sets the module system for the program - https://www.typescriptlang.org/tsconfig#module
    "moduleResolution": "node", // Specify the module resolution strategy - https://www.typescriptlang.org/tsconfig#moduleResolution
    "noEmit": true, // Do not emit compiler output files like JavaScript source code, source-maps or declarations - https://www.typescriptlang.org/tsconfig#noEmit
    "outDir": "dist", // If specified, .js (as well as .d.ts, .js.map, etc.) files will be emitted into this directory.
    "skipLibCheck": true, // Skip type checking of declaration files - https://www.typescriptlang.org/tsconfig#skipLibCheck
    "types": [
      "@testing-library/jest-dom",
      "jest",
      "node"
    ]
  },
  "exclude": [
    "dist",
    "node_modules"
  ],
  "include": [
    "src"
  ]
}